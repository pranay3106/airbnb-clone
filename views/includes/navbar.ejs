
<style>
  .btn-search{
    background-color: hsl(356, 99%, 63%);
    color: #fff;
    border-radius: 25px;
    padding: 0 1rem 0 1rem;
    line-height: 1rem;
  }
  .btn-search:hover{
    background-color: #fe424d;
    color: #fff;
  }

  .btn-search i {
    display: inline;
    margin-right: 0.5rem;
  }
  .search-inp{
    border-radius: 2rem;
  }
  .nv{
    width: 100%;
  }
</style>
<script>
  let currUser = JSON.stringify(res.locals.currUser) ;
</script>

<nav class="navbar navbar-expand-md bg-body-light border-bottom  sticky-top nv">
    <div class="container-fluid">
      <a class="navbar-brand" href="/listings"><i class="fa-regular fa-compass"></i></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
          <a class="nav-link" href="/listings">Explore</a>
          <!-- <a class="nav-link disabled" aria-disabled="true">Disabled</a> -->
        </div>
        <!-- search form -->
        <form class="d-flex navbar-nav ms-auto" method="GET" action="/listings/search">
  <input
    class="form-control me-2 search-inp"
    type="search"
    name="q"
    placeholder="Search listings"
    aria-label="Search"
    required
  />
  <button class="btn btn-search" type="submit">
    <i class="fa-solid fa-magnifying-glass"></i> Search
  </button>
</form>

     

        <!-- login sign up -->
         <div class="navbar-nav ms-auto">
          <a class="nav-link" href="/listings/new">Add new listings</a>
          <% if(!currUser){ %>
          <a class="nav-link " href="signup"><b>Sign up </b></a>
          <a class="nav-link" href="/login"><b>Log in</b></a>
          <% } %>

          <% if(currUser){ %>
          <a class="nav-link" href="/logout"><b>Log out</b></a>
          <% } %>
          <!-- <a class="nav-link disabled" aria-disabled="true">Disabled</a> -->
        </div>
      </div>
    </div>
  </nav>